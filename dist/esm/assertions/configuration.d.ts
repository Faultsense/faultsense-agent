import { Configuration } from "../types";
export declare function setConfiguration(config: Partial<Configuration>): Configuration;
export declare function isValidConfiguration(config: Configuration): boolean;
