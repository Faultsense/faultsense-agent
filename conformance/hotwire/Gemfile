source "https://rubygems.org"

gem "rails", "~> 8.1.3"
gem "puma", ">= 5.0"

# Hotwire: server-side Turbo Stream + Turbo Frame helpers. The Turbo
# JS client is loaded via a CDN <script> tag in the layout (we run with
# --skip-asset-pipeline --skip-javascript so there's no importmap to
# push it through).
gem "turbo-rails"

gem "tzinfo-data", platforms: %i[ windows jruby ]
