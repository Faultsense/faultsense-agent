<%# Populated by TodosController#create when validation fails. The
    conditional mutex on todos/add-item's error variant watches for a
    .add-error descendant of this slot. %>
<div id="add-error-slot">
  <p class="add-error"><%= message %></p>
</div>
